% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metaDyn-meta-var-mx.R
\name{MetaVARMx}
\alias{MetaVARMx}
\title{Fit Multivariate Meta-Analysis (metaVAR via OpenMx)}
\usage{
MetaVARMx(
  object,
  x = NULL,
  z = NULL,
  random = TRUE,
  alpha_free = NULL,
  alpha_values = NULL,
  alpha_lbound = NULL,
  alpha_ubound = NULL,
  tau_sqr_diag = FALSE,
  tau_sqr_d_free = NULL,
  tau_sqr_d_values = NULL,
  tau_sqr_d_lbound = NULL,
  tau_sqr_d_ubound = NULL,
  tau_sqr_l_free = NULL,
  tau_sqr_l_values = NULL,
  tau_sqr_l_lbound = NULL,
  tau_sqr_l_ubound = NULL,
  i_sqr_univariate = FALSE,
  gamma_free = NULL,
  gamma_values = NULL,
  gamma_lbound = NULL,
  gamma_ubound = NULL,
  kappa_free = NULL,
  kappa_values = NULL,
  kappa_lbound = NULL,
  kappa_ubound = NULL,
  phi_free = NULL,
  phi_values = NULL,
  phi_lbound = NULL,
  phi_ubound = NULL,
  omega_free = NULL,
  omega_values = NULL,
  omega_lbound = NULL,
  omega_ubound = NULL,
  psi_diag = FALSE,
  psi_d_free = NULL,
  psi_d_values = NULL,
  psi_d_lbound = NULL,
  psi_d_ubound = NULL,
  psi_l_free = NULL,
  psi_l_values = NULL,
  psi_l_lbound = NULL,
  psi_l_ubound = NULL,
  check_estimates = TRUE,
  effects = TRUE,
  set_point = TRUE,
  int_meas = TRUE,
  int_dyn = TRUE,
  cov_meas = TRUE,
  cov_dyn = TRUE,
  robust_v = FALSE,
  robust = FALSE,
  alpha = 0.05,
  seed = NULL,
  tries_explore = 100,
  tries_local = 100,
  max_attempts = 10,
  silent = FALSE,
  ncores = NULL
)
}
\arguments{
\item{object}{Output of the \code{\link[fitVARMxID:FitVARMxID]{fitVARMxID::FitVARMxID()}} function.}

\item{x}{An optional list.
Each element of the list is a numeric vector of covariates.}

\item{z}{An optional list.
Each element of the list is a numeric vector of distal outcomes.}

\item{random}{Logical.
If \code{random = TRUE},
estimates random effects.
If \code{random = FALSE},
\code{tau_sqr} is a null matrix.}

\item{alpha_free}{Logical vector.
Optional vector of free (\code{TRUE}) parameters for \code{alpha}.}

\item{alpha_values}{Numeric vector.
Optional vector of starting values for \code{alpha}.}

\item{alpha_lbound}{Numeric vector.
Optional vector of lower bound values for \code{alpha}.}

\item{alpha_ubound}{Numeric vector.
Optional vector of upper bound values for \code{alpha}.}

\item{tau_sqr_diag}{Logical.
If \code{tau_sqr_diag = TRUE},
\code{tau_sqr} is a diagonal matrix.
If \code{tau_sqr_diag = FALSE},
\code{tau_sqr} is a symmetric matrix.}

\item{tau_sqr_d_free}{Logical vector
indicating free/fixed status of the elements of \code{tau_sqr_d}.
If \code{NULL}, all element of \code{tau_sqr_d} are free.}

\item{tau_sqr_d_values}{Numeric vector
with starting values for \code{tau_sqr_d}.
If \code{NULL}, defaults to a vector of ones.}

\item{tau_sqr_d_lbound}{Numeric vector
with lower bounds for \code{tau_sqr_d}.
If \code{NULL}, no lower bounds are set.}

\item{tau_sqr_d_ubound}{Numeric vector
with upper bounds for \code{tau_sqr_d}.
If \code{NULL}, no upper bounds are set.}

\item{tau_sqr_l_free}{Logical matrix
indicating which strictly-lower-triangular elements
of \code{tau_sqr_l} are free.
Ignored if \code{tau_sqr_diag = TRUE}.}

\item{tau_sqr_l_values}{Numeric matrix
of starting values for the strictly-lower-triangular elements
of \code{tau_sqr_l}.
If \code{NULL}, defaults to a null matrix.}

\item{tau_sqr_l_lbound}{Numeric matrix
with lower bounds for \code{tau_sqr_l}.
If \code{NULL}, no lower bounds are set.}

\item{tau_sqr_l_ubound}{Numeric matrix
with upper bounds for \code{tau_sqr_l}.
If \code{NULL}, no upper bounds are set.}

\item{i_sqr_univariate}{Logical.
If \code{i_sqr_univariate = TRUE}, use the univariate formula for \eqn{I^2}.
If \code{i_sqr_univariate = FALSE}, use the multivariate formula for \eqn{I^2}.}

\item{gamma_free}{Logical matrix.
Optional matrix of free (\code{TRUE}) parameters for \code{gamma}.}

\item{gamma_values}{Numeric matrix.
Optional matrix of starting values for \code{gamma}.}

\item{gamma_lbound}{Numeric matrix.
Optional matrix of lower bound values for \code{gamma}.}

\item{gamma_ubound}{Numeric matrix.
Optional matrix of upper bound values for \code{gamma}.}

\item{kappa_free}{Logical vector.
Optional vector of free (\code{TRUE}) parameters for \code{kappa}.}

\item{kappa_values}{Numeric vector.
Optional vector of starting values for \code{kappa}.}

\item{kappa_lbound}{Numeric vector.
Optional vector of lower bound values for \code{kappa}.}

\item{kappa_ubound}{Numeric vector.
Optional vector of upper bound values for \code{kappa}.}

\item{phi_free}{Logical matrix.
Optional matrix of free (\code{TRUE}) parameters for \code{phi}.}

\item{phi_values}{Numeric matrix.
Optional matrix of starting values for \code{phi}.}

\item{phi_lbound}{Numeric matrix.
Optional matrix of lower bound values for \code{phi}.}

\item{phi_ubound}{Numeric matrix.
Optional matrix of upper bound values for \code{phi}.}

\item{omega_free}{Logical matrix.
Optional matrix of free (\code{TRUE}) parameters for \code{omega}.}

\item{omega_values}{Numeric matrix.
Optional matrix of starting values for \code{omega}.}

\item{omega_lbound}{Numeric matrix.
Optional matrix of lower bound values for \code{omega}.}

\item{omega_ubound}{Numeric matrix.
Optional matrix of upper bound values for \code{omega}.}

\item{psi_diag}{Logical.
If \code{psi_diag = TRUE},
\code{psi} is a diagonal matrix.
If \code{psi_diag = FALSE},
\code{psi} is a symmetric matrix.}

\item{psi_d_free}{Logical vector
indicating free/fixed status of the elements of \code{psi_d}.
If \code{NULL}, all element of \code{psi_d} are free.}

\item{psi_d_values}{Numeric vector
with starting values for \code{psi_d}.
If \code{NULL}, defaults to a vector of ones.}

\item{psi_d_lbound}{Numeric vector
with lower bounds for \code{psi_d}.
If \code{NULL}, no lower bounds are set.}

\item{psi_d_ubound}{Numeric vector
with upper bounds for \code{psi_d}.
If \code{NULL}, no upper bounds are set.}

\item{psi_l_free}{Logical matrix
indicating which strictly-lower-triangular elements
of \code{psi_l} are free.
Ignored if \code{psi_diag = TRUE}.}

\item{psi_l_values}{Numeric matrix
of starting values for the strictly-lower-triangular elements
of \code{psi_l}.
If \code{NULL}, defaults to a null matrix.}

\item{psi_l_lbound}{Numeric matrix
with lower bounds for \code{psi_l}.
If \code{NULL}, no lower bounds are set.}

\item{psi_l_ubound}{Numeric matrix
with upper bounds for \code{psi_l}.
If \code{NULL}, no upper bounds are set.}

\item{check_estimates}{Logical.
Check elements of \code{v} for positive definiteness.
If the test fails, the function generates a near positive definite matrix
to replace the original using \code{\link[Matrix:nearPD]{Matrix::nearPD()}}.}

\item{effects}{Logical.
If \code{effects = TRUE},
include estimates of the dynamic effects matrix, if available.
If \code{effects = FALSE},
exclude estimates of the dynamic effects matrix.}

\item{set_point}{Logical.
If \code{set_point = TRUE},
include estimates of the set-point vector, if available.
If \code{set_point = FALSE},
exclude estimates of the set-point vector.}

\item{int_meas}{Logical.
If \code{int_meas = TRUE},
include estimates of the measurement intercept vector, if available.
If \code{int_meas = FALSE},
exclude estimates of the measurement intercept vector.}

\item{int_dyn}{Logical.
If \code{int_dyn = TRUE},
include estimates of the dynamic process intercept vector, if available.
If \code{int_dyn = FALSE},
exclude estimates of the dynamic process intercept vector.}

\item{cov_meas}{Logical.
If \code{cov_meas = TRUE},
include estimates of the measurement error covariance matrix, if available.
If \code{cov_meas = FALSE},
exclude estimates of the measurement error covariance matrix.}

\item{cov_dyn}{Logical.
If \code{cov_dyn = TRUE},
include estimates of the process noise covariance matrix, if available.
If \code{cov_dyn = FALSE},
exclude estimates of the process noise covariance matrix.}

\item{robust_v}{Logical.
If \code{TRUE},
use robust (sandwich) sampling variance-covariance matrix
in stage 1.
If \code{FALSE},
use normal theory sampling variance-covariance matrix
in stage 1.}

\item{robust}{Logical.
If \code{TRUE},
calculate robust (sandwich) sampling variance-covariance matrix
in stage 2.}

\item{alpha}{NUmeric.
Alpha for test of significance and confidence intervals.}

\item{seed}{Random seed for reproducibility.}

\item{tries_explore}{Integer.
Number of extra tries for the wide exploration phase.}

\item{tries_local}{Integer.
Number of extra tries for local polishing.}

\item{max_attempts}{Integer.
Maximum number of remediation attempts
after the first Hessian computation fails the criteria.}

\item{silent}{Logical.
If \code{TRUE}, suppresses messages during the model fitting stage.}

\item{ncores}{Positive integer.
Number of cores to use.}
}
\description{
This function estimates fixed-, random-, or mixed-effects
meta-analytic parameters using per-individual coefficient estimates
and their sampling variance-covariance matrices.
Optionally, it fits distal-outcome models
in which between-person outcomes are regressed on
between-person covariates and the meta-analyzed parameters/effect sizes.
This function uses the estimated coefficients and
sampling variance-covariance matrix
from each individual fitted using the
\code{\link[fitVARMxID:FitVARMxID]{fitVARMxID::FitVARMxID()}} function.
}
\examples{
\donttest{
# Generate data using the simStateSpace package-------------------------
library(simStateSpace)
set.seed(42)
n <- 5
time <- 100
p <- 2
alpha <- rep(x = 0, times = p)
beta <- 0.50 * diag(p)
psi <- 0.001 * diag(p)
psi_l <- t(chol(psi))
mu0 <- simStateSpace::SSMMeanEta(
  beta = beta,
  alpha = alpha
)
sigma0 <- simStateSpace::SSMCovEta(
  beta = beta,
  psi = psi
)
sigma0_l <- t(chol(sigma0))
sim <- SimSSMVARFixed(
  n = n,
  time = time,
  mu0 = mu0,
  sigma0_l = sigma0_l,
  alpha = alpha,
  beta = beta,
  psi_l = psi_l
)
data <- as.data.frame(sim)

# Stage 1---------------------------------------------------------------
library(fitVARMxID)
stage1 <- FitVARMxID(
  data = data,
  observed = paste0("y", seq_len(p)),
  id = "id",
  center = TRUE
)
summary(stage1)
# Stage 2---------------------------------------------------------------
# Meta-analyze set point vector and matrix of lagged-effects
stage2 <- MetaVARMx(
  object = stage1,
  random = FALSE,
  effects = TRUE,
  set_point = TRUE,
  int_meas = FALSE,
  int_dyn = FALSE,
  cov_meas = FALSE,
  cov_dyn = FALSE
)
summary(stage2)
}

}
\references{
Cheung, M. W.-L. (2015).
\emph{Meta-analysis: A structural equation modeling approach}.
Wiley.
\doi{10.1002/9781118957813}

Neale, M. C., Hunter, M. D., Pritikin, J. N.,
Zahery, M., Brick, T. R., Kirkpatrick, R. M., Estabrook, R.,
Bates, T. C., Maes, H. H., & Boker, S. M. (2015).
OpenMx 2.0: Extended structural equation and statistical modeling.
\emph{Psychometrika},
\emph{81}(2), 535–549.
\doi{10.1007/s11336-014-9435-8}
}
\seealso{
Other Meta-Analysis of VAR Functions: 
\code{\link{Meta}()}
}
\author{
Ivan Jacob Agaloos Pesigan
}
\concept{Meta-Analysis of VAR Functions}
\keyword{meta}
\keyword{metaDyn}
